# acm_utils
Data structures and algorithms useful for programming competitions - and other pearls

## Development Setup

Install the git pre-commit hook to automatically format C/C++ files with `clang-format` before each commit:

```sh
hooks/install
```

Requires `clang-format` to be installed on your system.
